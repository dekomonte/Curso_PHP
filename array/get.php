<div class="titulo">$_GET</div> 

<p>Parâmetros passados na URL por uma requisição após a interrogação. Os parâmetros são separados por &.</p>

<p class="divisao">
    Definição do Manual do PHP: 
</p>

<p>Um array associativo de variáveis passadas para o script atual via os parâmetros da URL (também conhecidos como query string). 
    Note que o array não é preenchido apenas para solicitações GET, mas também para todas requisições com uma query string.</p>


<?php
//echo $_GET;
//echo '<br>';
//print_r($_GET);
//var_dump($_GET);
//echo "<br>{$_GET['nome']}";